{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"\nimport sys, json, shutil, os\ntry:\n    d = json.load(sys.stdin)\n    fp = d.get('tool_input', {}).get('file_path', '')\n    if fp.endswith('src/.my/my-resume.md'):\n        dest = os.environ.get('OMC_OBSIDIAN_RESUME', os.path.expanduser('~/Desktop/obsidian/resume/원본 이력서.md'))\\n        os.path.isdir(os.path.dirname(dest)) and shutil.copy(fp, dest)\n        print('\\033[32m[SYNC] src/.my/my-resume.md → Obsidian 원본 이력서.md 동기화 완료\\033[0m')\nexcept Exception as e:\n    print(f'[SYNC ERROR] {e}')\n\""
          }
        ]
      }
    ]
  }
}